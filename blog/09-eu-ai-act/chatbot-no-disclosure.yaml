# A customer-facing chatbot with NO Article 50 disclosure signal.
# valqore evaluate flags this as AIG-179.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-chatbot
  labels:
    app: chatbot
spec:
  replicas: 2
  selector: { matchLabels: { app: chatbot } }
  template:
    metadata:
      labels: { app: chatbot }
    spec:
      containers:
      - name: chatbot
        image: ghcr.io/danny-avila/librechat:v0.7.8
        resources:
          requests: { cpu: "250m", memory: "512Mi" }
          limits:   { cpu: "500m", memory: "1Gi" }
        ports:
        - containerPort: 3080
